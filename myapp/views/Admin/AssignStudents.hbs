<head>


    <style>
        .assign-container {
            background-color: white;
            margin-top: 10%;
            margin-left: 35%;
            margin-right: 30%;
            border-radius: 5px;
            overflow: hidden;
        }








        .form-control {
                      

            display: block;
            width: 100%;
            height: 34px;
            padding: 6px 12px;
            font-size: 14px;
            line-height: 1.42857143;
            color: #555;
            background-color: #fff;
            background-image: none;
            border: 1px solid #ccc;
            border-radius: 4px;
            -webkit-box-shadow: inset 0 1px 1px rgba(0, 0, 0, 0.075);
            box-shadow: inset 0 1px 1px rgba(0, 0, 0, 0.075);
            -webkit-transition: border-color ease-in-out .15s, -webkit-box-shadow ease-in-out .15s;
            -o-transition: border-color ease-in-out .15s, box-shadow ease-in-out .15s;
            transition: border-color ease-in-out .15s, box-shadow ease-in-out .15s;


        }





        .form-group {
            margin-bottom: 15px;

        }

        .help-block {
            display: block;
            margin-top: 5px;
            margin-bottom: 10px;
            color: #737373;
        }

        .assign-box {
            border-radius: 50%;
        }

        .title-header {
            color: black;
            font-size: .9em;
            display: flex;
            justify-content: left;
            padding: 2%;
            border-bottom: 1px solid gray;
            background-color: lightgray;
        }

        .assign-form {
            margin-top: 2%;
            padding: 2%;
        }

        input[type=submit] {
            width: 100%;
            background-color: #007bff;
            color: white;
            padding: 14px 20px;
            margin: 8px 0;
            border: none;
            border-radius: 4px;
            cursor: pointer;
        }

        input[type=submit]:hover {
            background-color: #008bff;
        }
    </style>
</head>
{{>header}}

{{>slideBar}}

<div class="assign-container">


    <div class="title-header">Assign Students to Supervisors</div>





    <form class="assign-form" action='assignStudents' method="POST">

        <div class="form-group row">

            <div>
                <p class="help-block">Please choose the Supervisor:</p>
                <select name="supervisor" class="form-control">

                    {{#each allSupervisors}}

                    <option value="{{_id}}">{{fullName}}</option>

                    {{/each}}



                </select>

            </div>
        </div>
        <br>
        <div class="form-group row">

            <!-- Type -->

            <div>
                <p class="help-block">Please choose the Student:</p>
                <select name="student" class="form-control">
                    {{#each allStudents}}

                    <option value="{{_id}}">{{fullName}}</option>



                    {{/each}}
                </select>

            </div>
        </div>
        <br>


        <input type="submit" value="Submit">


    </form>

</div>